"""
Transitional package that holds modularized tests during migration.
This is intentionally named `tests_pkg` to avoid colliding with Django's expected
`tracker.tests` module name. A shim `tracker/tests.py` will re-export from here.
"""
